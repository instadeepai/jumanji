defaults:
    - _self_
    - agent: random  # [random, a2c]
    - environment: rubiks_cube  # [binpack, cvrp, knapsack, routing, rubiks_cube, snake, tsp]

seed: 0
num_epochs: 10
num_learner_steps_per_epoch: 10
total_num_eval: 500

n_steps: 10
total_batch_size: 64
