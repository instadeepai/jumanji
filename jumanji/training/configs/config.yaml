defaults:
    - _self_
    - env: connector  # [binpack, cvrp, game2048, jobshop, knapsack, maze, minesweeper, rubiks_cube, snake, tsp, connector]

agent: random  # [random, a2c]

seed: 0

logger:
    type: terminal  # [neptune, tensorboard, terminal]
    save_checkpoint: false  # [false, true]
    name: ${agent}_${env.name}
